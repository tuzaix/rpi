/**
 * 项目配置文件
 * 注意：在实际生产环境中，建议将敏感信息放入 .env 文件
 */
export const APP_CONFIG = {
  // 后台管理登录配置
  admin: {
    username: 'admin',
    password: 'admin', // 请修改为您自己的密码
  },
  
  // 卡密配置
  license: {
    length: 12,
    prefix: '', // 已去掉 rpi- 前缀
  }
};
